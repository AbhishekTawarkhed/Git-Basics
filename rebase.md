# Git Rebase Workflow Guide

This document demonstrates how to use `git rebase` with a practical example, including file creation, conflict resolution, and working with remotes.

---

## 1. Initialize a Repository

```bash
mkdir rebase-demo
cd rebase-demo
git init
